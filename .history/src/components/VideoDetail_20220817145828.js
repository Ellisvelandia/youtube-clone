import React from "react";

import  { Paper, Typography} from '@material-ui/core';

const VideoDetail = ({video}) => {
  return (
    <React.Fragment></React.Frag>
   )
};

export default VideoDetail;
